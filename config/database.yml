default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: rails_saju_development
  url: 

test:
  <<: *default
  database: rails_saju_test
  url: 

production:
  primary:
    <<: *default
  cache:
    <<: *default
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    migrations_paths: db/queue_migrate  # 👈 여기가 중요! (작업 대기실 위치)
  cable:
    <<: *default
    migrations_paths: db/cable_migrate  # 👈 여기가 중요! (통신실 위치)